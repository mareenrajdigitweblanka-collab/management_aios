fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
psycopg[binary]>=3.1
pydantic>=2.6
python-dotenv>=1.0
openpyxl>=3.1
reportlab==5.0.0

# Test-only — required by fastapi.testclient.TestClient (backend/tests/
# test_calendar_auth.py and test_calendar_mutation_authorization.py use it
# for real HTTP-level authorization tests). Not imported by any production
# code path.
httpx>=0.27

# Test-only — required by backend/tests/test_review_summary_pdf_export.py
# to parse generated PDF bytes and extract text for content assertions
# (page count, heading/employee/reviewer text present, PDF signature).
# The application's own PDF module (backend/review_summary_pdf_export.py)
# only ever generates PDFs, never parses them — pypdf is never imported
# from any production code path.
pypdf==6.14.2
